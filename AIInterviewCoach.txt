1. GÜN

1.ASP.NET Core Web API projesi oluşturuldu (.NET8) :
	Backend geliştiriyoruz
	REST API yazacağız
	Frontend(Next.js) bu API ile iletişim kuracak

2. OpenAPI (Swagger) aktif edildi:
	Çünkü API'leri tarayıcıda test edebilmemizi sağlayacak

3.  Use Controllers işaretlendi :
	Çünkü kurumsal .NET projelerinin büyük kısmı Controller yapısı kullanır

4. Katmanlı Mimari kuruldu :
	Sebebi her katmanın ayrı bir sorumluluğu olsun istedik.
		AIInterviewCoach
		AIInterviewCoach.Domain
		AIInterviewCoach.Application
		AIInterviewCoach.Infrastructure

5.Domain katmanı oluşturuldu:
	Bu katman gerçek iş nesneleri için kullanılır.

6. Application katmanı oluşturuldu:
	Burada olacaklar:
		DTO'lar : Data Transfer Object
		Servisler
		İş kuralları
		Interfaces

7. Insfrastructure katmanı oluşturuldu:
	Burada olacaklar:
		Veritabanı
		Repository
		Entity Framework yani SQL server ile iletişim kuran katman

8. Referanslar bağlandı:
API
 ↓
Application
 ↓
Domain

Infrastructure
 ↓
Application
 ↓
Domain
sebebi: katmanların birbirine kontrollü olarak erişebilmesi gerekiyor

9. Entity Framework paketleri eklendi:

	EntityFrameworkCore: C# sınıflarını veritabanı tablolarıyla ilişkilendirmek için.
	SqlServer: Entity Framework’ün SQL Server ile çalışabilmesi için.
	Tools: Migration komutlarını kullanabilmek için.

10. AppContext sınıfı oluşturuldu:
	Persistance klasörüne AppContext.cs sınıfı ekledik. Bu sınıf projenin veritabanı ile bağlantı kurmasını sağlayan ana sınıftır.

11. AppContext içine Users tablosu tanımlandı
	public DbSet<User> Users { get; set; } satırı ile EFe dedik ki User entitysi veri tabanında Users tablosu olarak temsil edilecek.

12. appsettings.json içine connection stringi ekledin
"ConnectionStrings": {
  "DefaultConnection": "Server=(localdb)\\ProjectModels;Database=AIInterviewCoachDb;Trusted_Connection=True;TrustServerCertificate=True"
}
bu satır ile uygulamaya AIInterviewCoachDb isimli veritabanına LocalDB  üzerinden bağlanmasını söyledik.

13. Program.cs dosyasına DbContexti tanıttık:
builder.Services.AddDbContext<AppDbContext>(options =>
{
    options.UseSqlServer(
        builder.Configuration.GetConnectionString("DefaultConnection"));
}); ile ASP.NET Core uygulamasına "AppDbContext kullanılacağı zaman SQL Server bağlantısını appsettings.json’dan al." demiş olduk

14.İlk migration komutu çalıştırıldı:
	Veritabanı oluşturulmuş oldu

15. _EFMigrationHistory tablosu oluşturuldu:
	Bu tablo EF nin hangi migrationların veritabanına uygulandığındını takip etmesini sağlar

2. GÜN

1.LoginRequestDto oluşturuldu:
	kod mantığı:
public class LoginRequestDto
{
    public string Email { get; set; } = string.Empty;
    public string Password { get; set; } = string.Empty;
} şeklinde. Bunu yapma sebebimiz register işleminde Name, Email, Password alınıyordu. Ama loginde Name gerekmiyor bu yüzden yeni DTO oluşturuldu. 
	DTO'nun amacı: Kullanıcıdan gelen veriyi düzenli ve kontrollü şekilde karşılamak.

2.IAuthService içine LoginAsync metodu eklendi:
	Task<string> LoginAsync(LoginRequestDto request);
	IAuthService bizim sözleşmemiz gibi. Yani diyoruz ki Auth işlemleri yapan her servis, register yapabilmeli ve login yapabilmeli.

3.AuthService içine LoginAsync metodu yazıldı:
	Bu kısımda mantık şu önce email veritabanında aranıyor, email yoksa hata döndürülüyor. Eğer veritabanında bu emaile sahip kullanıcı yoksa user boş gelir sistem böyle bir kullanıcı yok der. Ama kullanıcıya direkt Bu email kayıtlı değil demiyoruz bunun yerine Email ya da şifre hatalı diyoruz çünkü kötü niyetli  biri sistemde hangi mailler var bunu öğrenemesin istiyoruz.

4. Kullanıcının girdiği şifreyi hashliyoruz. Veri tabanında register işlemi sırasında hashlanip kaydedilmiş olan şifre ile karşılaştırıyoruz doğruysa giriş başarılı dönüyor (şimdilik ileride JWT token dönecek). 

5.AuthController içine login endpoint eklendi:
[HttpPost("login")]
public async Task<IActionResult> Login(LoginRequestDto request)
{
    var result = await _authService.LoginAsync(request);

    if (result == "Email veya şifre hatalı.")
    {
        return Unauthorized(result); 
// unauthorized: Http tarafında karşılığı 401 Unauthorized Yanlış mail ya da şifre girilirse bu dönüyor.
    }

    return Ok(result); // Ok işlem başarılı demek HTTP karşılığı: 200 OK
}

Bu sayede dış dünyadan login isteği alabilmemiz sağlandı.

6. Şimdi JWT ekleyeceğiz:
	JWT, yani JSON Web Token, kullanıcının giriş yaptığını kanıtlayan dijital anahtar gibidir.

Kullanıcı login olur
↓
Email ve şifre doğruysa sistem token üretir
↓
Kullanıcı bu token’ı saklar
↓
Sonraki isteklerde token gönderir
↓
Backend token doğruysa kullanıcıya izin verir

Microsoft.AspNetCore.Authentication.JwtBearer paketini kuruyoruz. 

7.API projesindeki appsettings.json dosyasına Jwt bölümü ekliyoruz:

  "Jwt": {
    "Key": "AIInterviewCoachSuperSecretKey123456789!",
    "Issuer": "AIInterviewCoach",
    "Audience": "AIInterviewCoachUsers",
    "ExpireMinutes": 60
  },

Key, token’ı imzalamak için kullanılır. Issuer, token’ı kimin ürettiğini belirtir. Audience, token’ın kimler için üretildiğini belirtir. ExpireMinutes, token’ın kaç dakika geçerli olacağını belirtir.

8.AuthService içine IConfiguration eklendi:
	AuthService içinde appsettings.json dosyasındaki JWT ayarlarını okuyabilmek için IConfiguration kullandık.
private readonly AppDbContext _context;
private readonly IConfiguration _configuration;

public AuthService(AppDbContext context, IConfiguration configuration)
{
    _context = context;
    _configuration = configuration;
}

9.GenerateJwtToken metodu yazıldı: 
	AuthService içine token üreten özel metodu ekledik. Kullanıcının Id, Name ve Email bilgilerini token içine claim olarak ekledi. appsettings.json içindeki gizli key ile token’ı imzaladı. Token’a geçerlilik süresi verdi. Sonunda string olarak JWT token döndürdü.
private string GenerateJwtToken(User user)
{
    var claims = new[]
    {
        new Claim(ClaimTypes.NameIdentifier, user.Id.ToString()),
        new Claim(ClaimTypes.Name, user.Name),
        new Claim(ClaimTypes.Email, user.Email)
    };

    var key = new SymmetricSecurityKey(
        Encoding.UTF8.GetBytes(_configuration["Jwt:Key"]!));

    var credentials = new SigningCredentials(
        key,
        SecurityAlgorithms.HmacSha256);

    var expireMinutes = Convert.ToDouble(_configuration["Jwt:ExpireMinutes"]);

    var token = new JwtSecurityToken(
        issuer: _configuration["Jwt:Issuer"],
        audience: _configuration["Jwt:Audience"],
        claims: claims,
        expires: DateTime.Now.AddMinutes(expireMinutes),
        signingCredentials: credentials
    );

    return new JwtSecurityTokenHandler().WriteToken(token);
}

10. LoginAsync metodu token döndürecek şekilde güncellendi:
return Ok(new
{
    token = result
});

11. Program.cs içine şu kütüphaneleri ekledik:
using Microsoft.AspNetCore.Authentication.JwtBearer;
using Microsoft.IdentityModel.Tokens;
using System.Text;
Bunlar JWT token doğrulama işlemleri için gerekliydi.

12.program.cs içine authentication ayarları eklendi:
builder.Services.AddAuthentication(...)
    .AddJwtBearer(...);
Bu kodla ASP.NET Core şunu anladı: Bu projede kullanıcı doğrulama işlemi JWT Token ile yapılacak

13. Token doğrulama kuralları belirlendi:
ValidateIssuer = true;
ValidateAudience = true;
ValidateLifetime = true;
ValidateIssuerSigningKey = true;

Bunlar şu anlama geliyor: Token doğru sistem tarafından mı üretildi?
Token doğru kullanıcı grubu için mi üretildi?
Token’ın süresi dolmuş mu?
Token doğru gizli key ile mi imzalanmış?

14.Middleware sırası düzenlendi: Program.cs içinde 
app.UseHttpsRedirection();

app.UseAuthentication();

app.UseAuthorization();

app.MapControllers();

app.Run();
Burada sıra önemli çünkü Önce kullanıcı kim diye bakılıyor ardından bu kullanıcı bu endpointe girebilir mi kontrolü yapılıyor

Şu ana kadar sistem şu şekilde: 

Kullanıcı login olur
↓
Token alır
↓
Profile endpoint’ine gitmek ister
↓
Token gönderirse erişir
↓
Token göndermezse 401 Unauthorized alır

3. Gün
1. Mülakat için ana tabloları oluşturduk

Yeni entity’ler:

Position
InterviewSession
Question
Answer

Bunlar projenin ana iş mantığını taşıyacak.

2. User ile InterviewSession ilişkisini kurduk

User içine şunu ekledik:

public ICollection<InterviewSession> InterviewSessions { get; set; } = new List<InterviewSession>();

Bu şu demek:

Bir kullanıcının birden fazla mülakat oturumu olabilir.

3. AppDbContext’i güncelledik

Şu tabloları ekledik:

public DbSet<Position> Positions { get; set; }
public DbSet<InterviewSession> InterviewSessions { get; set; }
public DbSet<Question> Questions { get; set; }
public DbSet<Answer> Answers { get; set; }
4. Migration aldık ve veritabanını güncelledik

SQL Server’da artık şu tablolar var:

Users
Positions
InterviewSessions
Questions
Answers
__EFMigrationsHistory
5. Pozisyon modülünü yazdık

Şunları oluşturduk:

PositionDto
IPositionService
PositionService
PositionsController

Endpointler:

GET /api/Positions
POST /api/Positions/seed
6. Pozisyonları veritabanına ekledik

Seed endpoint’i ile şu pozisyonları ekledik:

Backend Developer
Data Analyst
DevOps Engineer
Business Analyst
Software Engineer
7. Pozisyonları Swagger’dan listeledik

GET /api/Positions ile pozisyonları başarıyla gördük.

8. Mülakat başlatma DTO’larını oluşturduk

Şunları ekledik:

StartInterviewRequestDto
InterviewSessionDto
9. Interview servis katmanını yazdık

Şunları oluşturduk:

IInterviewService
InterviewService

Bu servis artık seçilen pozisyona göre yeni mülakat oturumu oluşturabiliyor.

10. InterviewsController oluşturduk

Yeni endpoint:

POST /api/Interviews/start

Bu endpoint token’dan kullanıcıyı buluyor, seçilen PositionId ile yeni bir mülakat oturumu başlatıyor.

11. Swagger’dan mülakat başlatmayı test ettik

Token ile authorize oldun.

Sonra şu istek çalıştı:

{
  "positionId": 1
}

Ve sistem yeni InterviewSession oluşturdu.

4. Gün
SubmitAnswerRequestDto oluşturduk.
AnswerDto oluşturduk.
IInterviewService içine SubmitAnswerAsync metodunu ekledik.
InterviewService içine cevap kaydetme mantığını yazdık.
Sorunun gerçekten giriş yapan kullanıcıya ait olup olmadığını kontrol ettik.
Aynı soruya tekrar cevap verilirse cevabı güncelleyecek yapı kurduk.
Basit score hesaplama metodu ekledik.
Basit feedback üretme metodu ekledik.
InterviewsController içine POST /api/Interviews/answer endpoint’ini ekledik.
Answer entity’sindeki AnsweredAt alanını düzelttik.
Gerekirse migration ile veritabanını güncelledik.
Swagger’dan cevap gönderip veritabanında kaydedildiğini kontrol ettik.



